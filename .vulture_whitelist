# Vulture whitelist for ViberDash
# These are not actually dead code but used dynamically

# SQLite row_factory attributes - used by SQLite to configure row return format
_.row_factory

# Storage methods used by the main application and TUI
_.get_latest
_.get_previous  
_.cleanup_old_entries